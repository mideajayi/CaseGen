import { NextResponse } from "next/server";

export async function POST() {
  // Placeholder implementation for CaseGen draft generation.
  // Later, this will call OpenAI with the user's notes and screenshots.
  return NextResponse.json({ draft: "test" });
}

